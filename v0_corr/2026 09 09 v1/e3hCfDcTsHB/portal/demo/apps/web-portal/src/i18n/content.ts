import type { Locale } from './types'

/**
 * Значения из ядра переводу не подлежат.
 *
 * Раньше здесь лежал словарь условных данных: наименования продуктов,
 * производители, площадки и имена экспертов с переводами. Данные приходят из
 * ядра кабинета, и переводить их нельзя — наименование продукта в досье и в
 * интерфейсе должно совпадать буква в букву.
 *
 * Локализуются только строки интерфейса: они лежат в `catalog.ts`, а копия
 * права по работам — в `work-copy.ts`.
 */
export function contentText(value: string, _locale: Locale): string {
  return value
}
