import { Callout, Card, Empty, PageHeader } from '@demo/ui'

/**
 * Реестра счетов в API кабинета пока нет: ни чтения, ни операций оплаты.
 * Экран остаётся на месте, но показывает состояние канала, а не выдуманные суммы.
 */
export function LedgerPage() {
  return (
    <>
      <PageHeader title="Счета и платежи" />
      <Card>
        <Empty>Реестр счетов ядро пока не отдаёт: маршрута для него в API кабинета нет.</Empty>
      </Card>
      <Callout tone="quiet">
        Когда маршрут появится, строка комиссии и строка третьей стороны останутся разными типами записи: свести их в
        одну сумму нельзя.
      </Callout>
    </>
  )
}
